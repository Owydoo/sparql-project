export interface ResponseCardDto {
  // artistName?: string,
  // artistLink?: string,
  // artistDescription?: string,
  // albumList?: string,
  name?: string;
  link?: string;
  image?: string;
  description?: string;
}
